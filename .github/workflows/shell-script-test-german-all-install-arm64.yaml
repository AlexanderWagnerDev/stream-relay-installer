name: Test Installer Script in German with install all on arm64
on: 
  workflow_dispatch:
  
jobs:
  test-shell-script:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Run install script in German with install all on arm64
        run: |
          chmod +x ./install.sh
          ./install.sh <<EOF
          1
          1
          j
          j
          j
          j
          n
          j
          j
          j
          j
          EOF
